# Fabric Test Network

Prototype target:

- Single organization
- One peer
- One orderer
- CouchDB world state
- Real chaincode and transaction history

Use Fabric's test-network as the base deployment and add the evidence chaincode after the core capture pipeline is working.
